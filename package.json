{
  "name": "lukecjohnson.com",
  "version": "1.0.0",
  "scripts": {
    "start": "postcss src/main.css -o public/build/main.css --watch",
    "build": "postcss src/main.css -o public/build/main.css",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "autoprefixer": "^9.8.0",
    "cssnano": "^4.1.10",
    "postcss-cli": "^7.1.1"
  },
  "browserslist": [
    "defaults"
  ]
}
